# ============================================================================
# ENVIRONMENT CONFIGURATION - SECURITY BEST PRACTICES
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git - it's in .gitignore
# ============================================================================

# Application Mode
# Set to 'true' in production to hide API docs and enable strict error handling
PRODUCTION=true

# Security - Secret Key
# Generate with: openssl rand -hex 32
# CRITICAL: Change this in production!
SECRET_KEY=your-secret-key-here-change-in-production

# Domain Configuration for SSL
DOMAIN=yourdomain.com
EMAIL=your-email@example.com

# SSL/Let's Encrypt Configuration
CERTBOT_EMAIL=your-email@example.com
SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem

# CORS - Allowed Origins
# Comma-separated list of allowed domains
# Example: https://yourdomain.com,https://www.yourdomain.com
# Use "*" for development only (NOT recommended for production)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Trusted Hosts (for security)
TRUSTED_HOSTS=yourdomain.com,www.yourdomain.com

# Email Service (for contact form notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=Your Portfolio

# Database Configuration
DATABASE_URL=postgresql://portfolio_user:secure-postgres-password-123@db:5432/portfolio
POSTGRES_DB=portfolio
POSTGRES_USER=portfolio_user
POSTGRES_PASSWORD=secure-postgres-password-123

# Security Settings
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=10
SESSION_EXPIRE_MINUTES=30
COOKIE_SECURE=true
COOKIE_SAMESITE=strict

# Content Security Policy
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self' 'unsafe-inline' https://fonts.googleapis.com
CSP_STYLE_SRC='self' 'unsafe-inline' https://fonts.googleapis.com
CSP_FONT_SRC='self' https://fonts.gstatic.com
CSP_IMG_SRC='self' data: https:

# Analytics (optional)
GOOGLE_ANALYTICS_ID=

# Monitoring (optional)
SENTRY_DSN=

# Rate Limiting Storage
RATE_LIMIT_STORAGE=memory

# ============================================================================
# DEPLOYMENT INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate SECRET_KEY: openssl rand -hex 32
# 3. Update ALLOWED_ORIGINS with your domain
# 4. Set PRODUCTION=true for live deployment
# 5. Never commit .env to version control
# ============================================================================
